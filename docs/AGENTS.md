# Docs Agent Notes

This project follows the docs lifecycle under `docs/STATE.md`.

Shell commands in this workspace should use the local RTK wrapper:

```bash
rtk <command>
```

Version-specific design handoffs live in `docs/vN/designs/`.
Executable plans live in `docs/vN/plans/`.
